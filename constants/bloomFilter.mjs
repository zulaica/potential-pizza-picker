// TODO: Add a function to calculate the optimal number of hashing functions
// and bit vector length based on the number of potential items to be filtered
// instead of manually managing these values.
export const DEFAULTS = Object.freeze({
  HASH_COUNT: 6,
  OFFSET: 32,
  VECTOR_LENGTH: 217,
});
